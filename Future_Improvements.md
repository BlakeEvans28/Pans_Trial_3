# For BRANDT, BLAKE, WALKER (NOT FOR AI TO DO)
- Add maze picture for homescreen
-

# For AI
- Add a small visual hint on compact hand cards that says tapping opens Inspect before playing.
- Add a Plane Shift preview animation on the confirmation popup so the selected row or column shows where it will move before Apply Shift.
- Add regression-style UI smoke checks for Settings tutorial reset, compact hand-card Inspect, and Plane Shift confirmation.
- Add fallback warning text in Settings if a required art asset like `Trap.png`, `Ballista.png`, or `Stone_Wall.jpg` is missing.
- Add `PanTitle.png` to the background of the Title Screen. The png has the game title already, so the current text can be deleted.
- Remove all current audio in the game before these changes (except for the battle cling noise when players land on eachother)
- Add `Pan_Intro.mp3` to the intro title screen and drafting squence (looping)
- Add `PanPhase1.mp3` to the phase 1 + 2 sections of the game (looping)
- Play `Pan_Intro.mp3` on the victory screen as well (looping)
- Use `Pan_Icon.png` for the title screen button squares (start game, how to play, settings, quit). When a player hovers over them, make the png a brighter color

